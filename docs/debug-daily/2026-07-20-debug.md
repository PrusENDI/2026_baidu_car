# 2026-07-20 调试与修改记录

## 运行目录与同步

- 当前本地 worktree：`2026/baidu_smartcar_2026`
- 当前 Orin 运行副本：`/home/jetson/workspaces/baidu_car_2026_official_run_copy`
- 本地源码编辑，Orin 只用于同步、验证和硬件运行。

## 初始化导入链修复

修改文件：

- `smartcar/__init__.py`
- `smartcar/whalesbot/__init__.py`
- `smartcar/whalesbot/tools/__init__.py`
- `smartcar/paddlebaidu/__init__.py`

顶层 vehicle、tools、Paddle 和 Ernie 导入改为惰性导入；`CollectControlCar` 延迟导入，避免推理后端间接加载 vehicle 和唯一串口。旧 API（例如 `from smartcar import Camera`）保持兼容，原始导入代码均保留为注释。

Orin 验证结果：

```text
IMPORT_ISOLATION_OK
LAZY_API_OK Camera
```

## 推理后端进程管理修复

修改文件：`smartcar/paddlebaidu/infer_cs/base/infer_front.py`

`check_back_python()` 现在会：

- 检查真实脚本参数，排除 `python3 -c` 误匹配；
- 校验候选进程 cwd 是否属于当前项目；
- 校验 5001–5004 端口；
- 不复用其他 worktree 的 `infer_back_end.py`；
- 保留后端 stdout/stderr，使启动 traceback 可见；
- 打印 project root、PID、cwd、端口和启动失败诊断。

验证结果：`PROCESS_HELPERS_OK`。

## 视觉对齐假超时修复

修改文件：`car_wrap_2026.py`

第一轮播种的 `delta_x=-0.1`、`delta_y=-0.05` 现在按相对设定点比较；达到连续阈值后立即停车并返回检测结果，不再继续循环到 timeout。增加“视觉对齐完成”日志。

验证结果：

```text
Ran 2 tests
OK
```

## 右侧播种专用停车和视觉边界入口

修改文件：`car_task_function.py`、`car_wrap_2026.py`

仅在 `auto_seeding()` 第二轮右侧抓取中增加：

```python
RIGHT_SEEDING_PARK_OFFSET = (0.0, 0.0, 0.0)
RIGHT_SEEDING_ARM_X_PRESET = 0.30
RIGHT_SEEDING_ARM_Y_PRESET = 0.20
RIGHT_SEEDING_ARM_X_BOUNDS = (0.24, 0.315)
```

当前停车偏移为零，保留原停车点；其他任务、第一轮保存位姿和左侧放置逻辑不受影响。`arm_x_bounds=None` 时其他任务行为不变。

## 机械臂初始化遥测

修改文件：`smartcar/whalesbot/vehicle/arm/arm_base.py`

增加首次舵机角度命令前的 0.5 秒总线稳定等待，以及 `ARM_RESET`、`ARM_SERVO` 阶段日志。当前角度参数仍为 `RIGHT=-93°`、`UP=-90°`。

## 竖直轴状态修改与回退

曾尝试在每次 `move_y_position()` 前重置：

```python
# self.y_stop_flag = CountRecord(10)
# self.y_pid_flag = CountRecord(5)
# self.y_pose_now = self.y_get_position()
# self.y_pose_last = self.y_pose_now
# self.y_distance_change = 0
```

之后按用户要求已回退这些运行时重置行为，相关尝试和遥测保留为注释记录。当前 Orin 同步版本不执行这组竖直轴状态重置。

## 现场日志证据

- 第一轮保存位置：`arm_y≈0.2035`。
- 右侧抓取前：`arm_x≈0.31`，接近水平上限。
- 右侧 `dy` 有数据时由机械臂水平轴处理，不用于底盘 y 修正。
- `/dev/cam1` 曾出现读取超时，导致后续 `label=None` 和 `cylinder_set_list[None]` 崩溃。
- 水平轴 `move_x_position()` 的状态复用和 `x_pose_start` 自动重设问题尚未修改。

## 当前暂停状态

- 用户要求暂停进一步修改。
- 当前没有继续修改水平轴或竖直轴。
- 没有运行新的真实 `auto_seeding()`。
- 未提交、未 push；现有未跟踪文档未加入提交。
