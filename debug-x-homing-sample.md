# X 轴归零限位采样调试

- Session: `x-homing-sample`
- Status: `[OPEN]`
- Symptom: 初始化调用 `reset_position()` 时，MC602 未返回新的 X 轴限位样本，机械归零失败。
- Scope: 先仅增加观测日志，不修改归零业务逻辑。

## 假设

1. MC602 限位状态未产生新的采样时间戳。
2. 控制器限位读取接口持续返回旧缓存。
3. X 电机运动但未在超时内触发限位。
4. 控制器初始化与首次限位读取存在时序问题。

## 已有证据

- 串口与 MC602 初始化成功。
- 新鲜采样检查有效，AI1 每次读取均更新 `last_data`。
- 本次归零期间 X 编码器从起点移动到 `-0.197482 m`，随后停滞。
- AI1 在本次运动全程保持 `0`，未达到 `2000` 的触发阈值。
- 前一次采集中曾短暂出现 `4028/4002/4026`，说明端口和触发极性可读取到高电平，但本次机械到端后没有触发。

## 判断

- 假设 1、2、4已排除：采样持续刷新，不是旧缓存或初始化时序问题。
- 假设 3成立：X 轴到达机械端并停滞，但微动开关未稳定触发。
- 当前更符合开关压片未被顶到、位置偏移、接线接触不稳或开关本体间歇失效。
- 不可把编码器停滞直接当作零点，否则堵转或异物卡住时会建立错误零点。
