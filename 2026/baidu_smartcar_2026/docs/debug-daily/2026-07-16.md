# 2026-07-16

## 录制循迹测试交接

- 已确认本地隔离 worktree 位于 `lane-test-telemetry` 分支，开始时工作树干净。
- 本轮未修改控制、模型或录制代码；未向 Orin 同步文件，未在 Orin 运行命令。
- Orin 上已同步的 `tools/safe_lane_test.py` 预检成功。等待用户以 `--speed 0.05 --duration 10 --startup-timeout 30 --record-dir logs/lane-tests` 完成一次低速实车录制。
- 收到明确的一次录制目录和终端输出后，只拉取该目录至本地 `logs/orin/`，分析 `metadata.json`、`frames.csv` 与 `annotated.mp4`，再提出证据驱动的参数建议。

## Orin 日志拉取

- 用户授权直接拉取 Orin 运行副本的整个 `logs/` 目录；未向 Orin 写入或同步任何源码。
- 已拉取并按字节数校验至本地 `logs/orin/lane-tests/`：`20260716T092014Z-1cd04362` 的 `annotated.mp4`、`frames.csv`、`metadata.json`，以及 `20260716T092527Z-2bde3409` 的 `annotated.mp4`、`frames.csv`。
- 第二次录制在 Orin 上当前没有 `metadata.json`，属于源端现状；本地与 Orin 共五个文件的路径和大小一致。

## 新采集数据的离线初步分析（未改参）

- 已读取两份 `frames.csv`：第一次 `20260716T092014Z-1cd04362` 为 577 帧、9.996 秒、`vx=0.05`，并由元数据确认停止原因为 `duration_elapsed`；第二次 `20260716T092527Z-2bde3409` 为 4,507 帧、77.7 秒、`vx=0.1`，超过原定低速 10 秒测试范围，且源端没有对应 `metadata.json`，因此尚不能确认停止原因。
- 两段推理均无超时或性能瓶颈：推理耗时均值分别为 10.49 ms 与 10.32 ms，最大为 14.23 ms 与 15.34 ms；没有大于 50 ms 的帧，也没有低于 20 FPS 的帧。首帧的极高 FPS 为计时基准值，统计时应排除。
- 横向模型/横移控制量持续很小且略为负值：第一次 `error_y` 均值 -0.00009、`vy` 均值 -0.00049；第二次分别为 -0.00008、-0.00042。仅从记录无法据此证明车辆实际左压线，需与后方/上方外部视频或可视觉核验的车载画面比对。
- 航向控制是主要动态量：第一次 `yaw` 最大 1.250，第二次范围 -1.500 至 1.431；第二次在约 12.13--12.76 秒出现连续负向大转向并触及 -1.5，约 34.23--34.52 秒、44.14--44.52 秒等区间出现正向大转向。对应 `error_angle` 峰值约为 -0.512 与 0.477，符合弯道/锐角或视觉车道角度突变，尚未判定为 PID 参数问题。
- 分析尚未完成：本地随附 Python 没有 `cv2`，也没有可用 ffmpeg，故关键时段的 MP4 抽帧视觉核验未完成；已明确暂停在证据收集阶段，不修改 PID、横向补偿、模型或锐角策略。

## 今日修改文件完整内容

- 已归档今天提交所涉及文件在当前 `HEAD` 的完整内容，见 [2026-07-16-modified-files.md](2026-07-16-modified-files.md)：`.gitignore`、`docs/superpowers/specs/2026-07-16-lane-test-telemetry-design.md`、`tools/safe_lane_test.py`、`tests/test_safe_lane_test.py`。
- 今天未提交的工作树修改仅为本日报及该内容归档；拉取的 `logs/orin/` 为运行产物，按 `.gitignore` 忽略，未纳入源码修改清单。

## GitHub 同步核验

- 本地 `lane-test-telemetry` 分支停在 `9665a7f`，未配置上游分支；今天的录制与安全清理相关提交均位于该本地分支，不能视为已推送至 GitHub。
- 主 worktree 的本地 `main` 显示相对记录的 `origin/main` 领先 6 个提交，说明至少有 6 个本地提交尚未由该跟踪状态反映在 GitHub。
- 尝试只读查询 GitHub 远端分支头失败：当前会话没有可用的 GitHub HTTPS 凭据，受控网络通道也无法读取 `origin`。因此无法以远端实时状态确认是否存在同名远端分支；未执行 `push`、`fetch`、提交或任何远端写入操作。
