# 2026-07-20 Orin 2026-07-17 副本合并记录

## 来源与范围

- 来源副本：`C:/weizijian/documents/baidu/orin-backups/20260720-baidu_smart_2026_7_17`
- Orin 来源：`/home/jetson/workspaces/baidu_smart_2026_7_17/`
- 副本验证：544 个文件，rsync checksum dry-run 无内容差异。
- 合并原则：只合并 2026-07-17 目录中后续修改的源码和配置；不复制模型、日志、缓存和运行产物。
- 当前 worktree 的播种遥测保留，不覆盖现有未跟踪文档。

## 修改前代码与副本代码

### `car_start_2026.py`

修改前 worktree 会执行完整任务链：

```python
init()
auto_seeding()
animal_list = target_shooting_detection()
water_tower_task()
target_shooting(animal_list)
crop_harvesting()
sort_and_store()
order_list = get_order()
order_delivery(order_list)
```

副本只自动执行 `init()` 和 `auto_seeding()`，其余入口保留为注释，并新增
`auto_lane_tracing()` 供独立巡线调试。

### `car_task_function.py`

| 阶段 | 修改前 worktree | Orin 2026-07-17 副本 |
|---|---|---|
| 初始手部 | `DOWN` | `UP` |
| 第一轮视觉偏置 | 默认 `(0, 0)` | `delta_x=-0.1, delta_y=-0.05` |
| 抓取后抬升 | `0.2 m` | `0.1 m` |
| 放置前顺序 | 先恢复 x，再切换 LEFT | 先切换 LEFT、等待 2 秒，再恢复 x |
| 结束手部 | `UP` | `DOWN` |

播种遥测函数和所有阶段日志继续保留。

### `car_wrap_2026.py`

| 项目 | 修改前 worktree | Orin 2026-07-17 副本 |
|---|---|---|
| `get_target_location()` | `CAMERA_* * x_c/y_c` | 再加 `w/2`、`h/2` |
| 对齐超时 | `2.0 s` | `10.0 s` |
| 达到阈值 | 立即 return | 停车后继续至 timeout |
| 默认机械臂补偿 | `0.01 m` | `0.03 m` |

`get_target_location()` 当前没有 `auto_seeding()` 调用方，坐标差异暂按副本保留并在
源码中标明需单独标定。

### `config_car.yml`

```text
修改前：cfg_pid_y.Kp=5，cfg_pid_angle.Kp=3
副本值：cfg_pid_y.Kp=4，cfg_pid_angle.Kp=2
```

### `infer_wrap.py`

修改前在模块导入阶段加载：

```python
from .deploy.pptracking.python.mot_sde_infer import SDE_Detector
from .deploy.pipeline.pphuman.attr_infer import AttrDetector
from .deploy.pipeline.ppvehicle.vehicle_plate import PlateRecognizer, PlateDetector, TextRecognizer
```

副本把依赖移动到 `MotHuman`、`HummanAtrr`、`OCRReco` 构造函数内，避免普通播种
初始化提前加载 MOT/sklearn，并规避已观测到的 `libgomp` static TLS 错误。

### `arm_base.py`

| 项目 | 修改前 worktree | Orin 2026-07-17 副本 |
|---|---|---|
| 位置误差阈值 | `4e-4` | `1e-3` |
| 停止检测阈值 | `1e-10` | `1e-4` |
| 抓取泵控制 | `pump.set(not value)` | `pump.set(value)` |
| 抓取阀控制 | `valve.set(value)` | `valve.set(not value)` |
| 便捷属性 | 无 | `x/y/angle/hand_angle` |

### `arm_cfg.yaml`

角度标定保持副本值 `RIGHT=-93`、`UP=-90`。保存姿态从 worktree 的
`horiz=0, vert=0.2001525, RIGHT` 更新为副本的
`horiz=0.012703908093849785, vert=0.20050500000000004, LEFT`。

## 未覆盖的基线差异

`.gitignore`、`README.md`、`camera.py` 和 `mecanum.py` 的副本时间接近目录创建时间，
未判定为后续现场修改；当前 worktree 中的同步文档、摄像头回退和本地底盘接口继续
保留。若后续确认这些文件也属于现场修改，再单独审查合并。
